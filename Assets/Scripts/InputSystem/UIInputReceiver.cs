using UnityEngine;

/*
-------------------------------------------
Input receiver for the UI
-------------------------------------------
*/

namespace InputSystem
{
    public class UIInputReceiver : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
